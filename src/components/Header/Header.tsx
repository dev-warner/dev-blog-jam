import * as React from 'react';

import './header.scss';

export const Header = () => {
  return (
      <nav className='navigation'>
        <div className="navigation__brand" aria-label={'Amplience - Amplify Expirence Logo'}></div>
      </nav>
  )
}
